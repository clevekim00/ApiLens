# Web 构建指南

### 构建步骤
1. `flutter pub get`
2. `dart run build_runner build --delete-conflicting-outputs`
3. `flutter build web --release`

### 结果
`build/web/` (将这些文件托管在任何静态 Web 服务器上)。

---
其他语言: [English](BUILD_WEB.md) | [한국어](BUILD_WEB_KO.md)
