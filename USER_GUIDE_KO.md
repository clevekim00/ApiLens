# ApiLens 사용법 가이드

ApiLens는 노드 기반의 직관적인 인터페이스를 통해 API 워크플로우를 설계, 테스트 및 관리할 수 있는 강력한 도구입니다.

---

## 1. 시작하기: API 명세 가져오기

ApiLens의 첫 번째 단계는 작업할 API 명세를 가져오는 것입니다.

### Swagger (OpenAPI) URL로 가져오기
- **자동 감지 기능**: Swagger UI 주소(예: `http://localhost:8080/swagger-ui/index.html`)를 입력하면 ApiLens가 실제 명세 주소를 자동으로 찾아냅니다.
- **Glassmorphism 인터페이스**: 세련된 카드 형태의 진입 화면에서 URL을 입력하거나 파일을 업로드하세요.

### 로컬 파일로 가져오기
- `JSON` 또는 `YAML` 형식의 OpenAPI 명세 파일을 직접 업로드할 수 있습니다.
- 태그별 필터링 기능과 검색 기능을 통해 필요한 엔드포인트만 선택하여 워크플로우를 생성하거나 개별 요청으로 저장할 수 있습니다.

---

## 2. 탐색기 및 워크그룹 관리

왼쪽 사이드바의 **Workgroup Explorer**에서 모든 자산을 관리합니다.

- **폴더 구조**: 우클릭 또는 상단 버튼을 통해 폴더를 생성하고 워크플로우와 요청을 계층별로 정리할 수 있습니다.
- **가져오기/내보내기**: 폴더 단위로 데이터를 내보내거나(JSON) 다른 사용자가 공유한 파일을 가져올 수 있습니다.
- **Hover 액션**: 항목 위에 마우스를 올리면 삭제 등 빠른 작업을 수행할 수 있는 버튼이 나타납니다.

---

## 3. 워크플로우 편집기

워크플로우 편집기는 노드를 연결하여 API 비즈니스 로직을 설계하는 공간입니다.

### 사용 가능한 노드 유형
- **Start**: 워크플로우의 시작점입니다. (필수)
- **API Request**: HTTP 요청을 수행합니다. 임포트한 엔드포인트 정보가 자동으로 포함됩니다.
- **Condition**: 조건문 노드로, 결과에 따라 경로를 분기합니다.
- **End**: 워크플로우가 종료되는 지점입니다.
- **GraphQL/WebSocket**: 특수한 통신 프로토콜을 위한 전용 노드입니다.

### 기본 조작법
- **노드 추가**: 왼쪽 팔레트에서 노드를 클릭하여 캔버스에 추가합니다.
- **노드 연결**: 한 노드의 출력점을 드래그하여 다른 노드의 입력점에 연결합니다.
- **노드 속성 수정**: 노드를 클릭하면 오른쪽 **Inspector Panel**에서 상세 설정(URL, Headers, Body 등)을 변경할 수 있습니다.

---

## 4. 실행 및 디버깅

워크플로우를 실행하여 로직이 제대로 작동하는지 확인합니다.

- **유효성 검사**: 실행 시 Start에서 End까지 도달 가능한 경로가 있는지 자동으로 확인합니다.
- **실행(Run)**: 상단 툴바의 '재생' 버튼을 누르면 워크플로우가 순차적으로 실행됩니다.
- **디버그 패널**: 하단 패널에서 각 단계별 실행 결과, 응답 값 및 로그를 실시간으로 모니터링할 수 있습니다.

---

## 5. 필수 단축키 (Keyboard Shortcuts)

생산성을 높여주는 필수 단축키 목록입니다.

| 기능 | 단축키 (Mac) | 단축키 (Windows/Linux) |
| :--- | :--- | :--- |
| **저장 (Save)** | `Cmd + S` | `Ctrl + S` |
| **다른 이름으로 저장** | `Cmd + Shift + S` | `Ctrl + Shift + S` |
| **새 워크플로우** | `Cmd + N` | `Ctrl + N` |
| **워크플로우 열기** | `Cmd + O` | `Ctrl + O` |
| **워크플로우 실행** | `Cmd + Enter` | `Ctrl + Enter` |

---

## 6. 데이터 공유 (Export JSON)

작성한 워크플로우를 JSON 형식으로 내보내어 팀원과 공유하거나 백업할 수 있습니다.
'Export JSON'을 클릭하면 **미리보기 팝업**이 나타나며, 여기서 내용을 복사하거나 파일로 직접 저장할 수 있습니다.
